def readthedocs(project_slug: str) -> None:
    base_url = "https://readthedocs.org/api/v3"
    project_slug = "your-project-slug"  # Replace with your project's slug
    version_slug = "latest"  # Replace with the desired version slug
